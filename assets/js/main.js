const themeToggle=document.querySelector('.theme-toggle');const KEY='thien-site-theme';function applyTheme(t){document.body.classList.toggle('dark-mode',t==='dark');if(themeToggle){themeToggle.title=t==='dark'?'Switch to light mode':'Switch to dark mode';themeToggle.setAttribute('aria-label',themeToggle.title)}}const saved=localStorage.getItem(KEY);const prefers=window.matchMedia&&window.matchMedia('(prefers-color-scheme: dark)').matches;applyTheme(saved||(prefers?'dark':'light'));if(themeToggle){themeToggle.addEventListener('click',()=>{const next=document.body.classList.contains('dark-mode')?'light':'dark';localStorage.setItem(KEY,next);applyTheme(next)})}